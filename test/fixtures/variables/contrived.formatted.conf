# contrived: brace-wrapped variables next to real blocks
listen      ${NGINX_PORT};
server_name ${NGINX_HOST}.com ${A}${B} $c$d;

set $x $ {
    y 1;
}

set         $z b$ c;
proxy_pass  http://$host$request_uri;
