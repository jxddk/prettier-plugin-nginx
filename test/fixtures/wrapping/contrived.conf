# contrived: many parameters, a parameter wider than the line, padding that causes overflow
server_name a.example.com b.example.com c.example.com d.example.com e.example.com f.example.com g.example.com;
try_files $uri $uri/ /index.php?$query_string /fallback/one /fallback/two;
add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' https://cdn.example.com; img-src *";
a_directive_with_a_long_name x;
log_format main '$remote_addr - $remote_user [$time_local] "$request"' '$status $body_bytes_sent "$http_referer"' '"$http_user_agent"';
return 200 'line one
line two';
location / { proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; }
