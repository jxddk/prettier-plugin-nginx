# contrived: comments in every position, glued to or detached from their neighbours
## double   
#no-space
#
user www; ## trailing
events { # opening
    worker_connections 1024; # trailing
} # after block
# glued to http
http {
    # first in block

    include mime.types; # trailing

    # detached paragraph

    # glued to server
    server { # opening
        listen # between name and parameter
          80;
        return 200 # before the semicolon
        ;
        server_name a # between parameters
            b;
    } # after block
    # last in block
}
# last in file
