# contrived: odd spacing, quoted operands, single-token and unparenthesised conditions
if ( $request_method = POST ) { return 405; }
if ( "$slow" ) { return 503; }
if ( "$http_x_forwarded_proto" != "https" ) { return 301 https://$host$request_uri; }
if ($x ~ "a b") { return 1; }
if ($x !~* "^(?:a|b){2}$") { return 2; }
if (!-e $request_filename) { rewrite ^ /index.php last; }
if($y){ return 3; }
if $z { return 4; }
if ($a) { if ($b) { return 5; } }
if ( $c # comment
) { return 6; }
