# contrived: alignment, wrapping and Lua under tab indentation
http {
	sendfile on;
	server_names_hash_bucket_size 128;
	server {
		server_name a.example.com b.example.com c.example.com d.example.com e.example.com f.example.com;
		content_by_lua_block {
			local t = {
				a = 1,
			}
			ngx.say(t.a)
		}
	}
}
