# contrived: minified input, tabs, carriage returns, braces glued to words, empty blocks
http{server{listen 80;location /{root html;}location /a{}location /b{ }}}
	events	{	worker_connections	1024;	}
set $x foo}bar;
location /c{root /x;}
http { server { location / { if ($x) { location /d { limit_except GET { deny all; } } } } } }
